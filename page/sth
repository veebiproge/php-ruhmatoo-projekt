sth
SELECT * FROM 
(SELECT id, firstname, lastname, email, phonenumber, date_of_birth, saved, baptised FROM people WHERE firstname LIKE "%Admin%") AS t1 
JOIN (SELECT line_of_work, person FROM l_o_wOnPpl WHERE person = id) AS t2 ON t1.id = t2.person